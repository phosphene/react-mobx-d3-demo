import * as d3 from 'd3';
//we can call export at the top of the class declaration
export default class CaliMapD3 {

  constructor(el, props = {}) {
    //we initiate charts in constructor

  }

  render() {
    //de-structure myCharts object
    }

  //end of class
}
